// CartItem interface defining the structure
interface CartItem {
  product: string;
  quantity: number;
  image: string;
  name: string;
  price: number;
  inStock: number;
}

export default CartItem;
